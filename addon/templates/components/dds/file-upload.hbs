<p>
  {{#if documents.length}}
    <ul class="au-tags files">
      {{#each documents as |document index|}}
        <li class="files {{theme}}">
          <a class="file">
            {{document.original_filename}} ( {{document.extension}} , {{number-to-human-size document.size}} )
          </a>
          {{#unless disabled}}
            <a class="pointer right" onclick={{action "removeDocument" index}}>
              x
            </a>
          {{/unless}}
        </li>
      {{/each}}
    </ul>
  {{else}}
    No file selected
  {{/if}}
  <br>
  {{#unless disabled}}
    {{#file-upload name=name
                   accept=(if fileTypes fileTypes (array "image/jpeg" "image/png" "application/pdf"))
                   multiple=multiple
                   onfileadd=(action "uploadDocument")}}
      <a class="au-btn au-btn--tertiary {{if theme (concat "au-btn--tertiary--" theme)}}">
        {{#if (and documents.length multiple)}}
          Add another file
        {{else}}
          {{fa-icon "plus-circle" prefix="fas"}} Choose a file
        {{/if}}
      </a>
    {{/file-upload}}
  {{/unless}}
</p>
<div class="clear-both"></div>
{{#if (and (or showError hasChanged) invalid)}}
  {{dds/validation-error type="error" message=errorMessage float="left"}}
{{else if (and apiError (not hasChanged))}}
  {{dds/validation-error type="warning" message=apiError float="left"}}
{{/if}}
